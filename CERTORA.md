# Certora Verification Guide


- Run `just build` from `blend-contracts` root
- All conf files are in the `blend-contracts/confs` directory.
- You can run them like so: `certoraRun.py confs/name.conf`
